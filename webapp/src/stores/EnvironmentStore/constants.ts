/**
 * A map from theme type to css stylesheet location.
 */
export const themes = {
  light: 'https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css',
  dark: '/bootstrap.dark.css',
} as const;
