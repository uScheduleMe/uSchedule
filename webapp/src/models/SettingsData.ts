export interface SettingsData {
  /**
   * If true, display time as 24 hour syntax
   */
  format_time_as_military: boolean;

  /**
   * If true, use the remote scheduler instead of the local scheduler
   */
  use_remote_scheduler: boolean;
}
